using Microsoft.EntityFrameworkCore;

namespace ASHWebAPI.dbContext
{
    public class ASHDbContext : DbContext
    {
        //I am just pseudo code for a database context, pretend I do everything I need to do to talk to a data base :D
        // I am a database that contains tables shaped like the Models objects - see (database diagram) for the database architecture
    }
}
